instance_groups:
- name: bosh
  vm_type: production-bosh
  persistent_disk_type: production-bosh
  vm_extensions: [production-bosh-profile]
  networks:
  - name: production-bosh
    static_ips:
    - (( grab terraform_outputs.bosh_static_ip ))
